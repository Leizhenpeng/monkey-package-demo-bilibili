{
	"name": "bilibili-monkey",
	"version": "0.0.7",
	"files": [
		"dist"
	],
	"scripts": {
		"dev": "monkey dev",
		"build": "monkey build",
		"prepare": "monkey prepare",
		"release": "bumpp && monkey build && npm publish --no-git-checks",
		"publish": "monkey publish"
	},
	"devDependencies": {
		"bumpp": "^9.6.1",
		"next-monkey": "link:/Users/river/dev/tsPool/monkey"
	},
	"dependencies": {
		"ext-msg": "^0.0.8",
		"human-being": "^0.0.5"
	}
}
